[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

# Libraries
lib_deps =
    # Add your dependencies here
    # e.g., WiFi, sensor libraries, etc.

# Build options
build_flags =
    -DCORE_DEBUG_LEVEL=1

# Upload settings
upload_speed = 921600
